import { IdadeMinimaDirective } from './idademinima.directive';

describe('IdadeMinimaDirective', () => {
  it('should create an instance', () => {
    const directive = new IdadeMinimaDirective();
    expect(directive).toBeTruthy();
  });
});
